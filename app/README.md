# App

## Install

* Install npm dependencies
```
$ npm install
```

## Run
* In one terminal run
```
$ react-native start
```

* Open another terminal in the project directory
```
$ react-native run-[android | ios]
```
